<li class = "spacing">

	{{#if devoured}}
	{{id}}. {{burger_name}}
	{{else}}
	{{id}}. {{burger_name}}
	<button class="devour-btn btn btn-light" type="button" data-id="{{id}}" data-newDevour="{{devoured}}">
		Devour it!
	</button>
	{{/if}}

</li>